server:
    port: 8075

# API Gateway routes configuration
spring:
  cloud:
    gateway:
      routes:
        - id: guest-service-route
          uri: lb://GUEST-SERVICE
          predicates:
            - Path=/api/guests/**
        - id: reservation-service-route
          uri: lb://RESERVATION-SERVICE
          predicates:
            - Path=/api/reservations/**
        - id: room-service-route
          uri: lb://ROOM-SERVICE
          predicates:
            - Path=/api/rooms/**

